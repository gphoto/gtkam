pixmapsdir = get_option('prefix') / get_option('datadir') / 'gtkam' / 'pixmaps'

install_data(
  'camera.xpm',
  'configure.xpm',
  'delete_images.xpm',
  'exit.xpm',
  'folder.xpm',
  'save_current_image.xpm',
  install_dir: pixmapsdir,
)